.container
  - if @tasks.empty?
    %span.text-warning There are no tasks at the moment!
  - else
    %table.table.table-hover.table-bordered
      %thead
        %tr
          %th Title
          %th Created at
          %th Completed
      %tbody
        - @tasks.each do |task|
          %tr
            %td
              %strong= task.title
            %td.text-info= task.created_at
            %td.text-info= task.completed
#modal.modal.fade
